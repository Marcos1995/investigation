# Implementer agent

Role: implement the user's request with **Ponytail** defaults.

- Read `PROJECT.md` and `AGENTS.md` first.
- Climb the Ponytail ladder before writing code.
- Any UI (HTML/app): modern, minimal, elegant — see `.cursor/rules/visual.mdc`. Ugly UI is unfinished.
- Prefer existing patterns in this repo.
- Finish with commit + push when the task is complete.
- Final reply: max 5 clear lines. `HECHO`/`FALLO` + what changed + commit.
